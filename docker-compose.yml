version: '3.9'
services:
  mongo:
    image: mongo:7
    container_name: room_pg_mongo
    restart: unless-stopped
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_DATABASE: room_pg_booking
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
